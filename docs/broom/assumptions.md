# Assumptions & decisions

Plan rule 7: document assumptions. Add an entry per stage as decisions are made.

| Date | Stage | Assumption / decision | Why |
|------|-------|-----------------------|-----|
| 2026-09-14 | scaffold | Neuron model v1 = Leaky Integrate-and-Fire (no Hodgkin-Huxley). | Plan §2.3. |
| 2026-09-14 | scaffold | Connectome topology is fixed; only synaptic weights are trainable. | Plan §16. |
| 2026-09-14 | scaffold | No large ML model between environment and brain; adapters are simple transforms. | Plan §2.1. |
| 2026-09-14 | scaffold | CPU-only, NumPy. No GPU/Rust until the hypothesis works. | Plan §42. |
| 2026-09-14 | scaffold | ViZDoom/Gymnasium are not dependencies yet; added at stage 18. | Plan §44. |
| 2026-09-14 | 0 | Synchronous update: a step reads activities from the previous tick, so a signal travels one synapse per tick and neuron order is irrelevant. | Determinism; matches Plan §40 replay requirement. |
| 2026-09-14 | 0 | Activity is binary (0/1) and potential resets to 0 on firing. Leak is applied before input: `p = p*(1-decay) + I`. | Simplest model that satisfies Plan §6 acceptance criteria. |
| 2026-09-14 | 0 | Pure Python dicts, no NumPy in the brain yet. | 3 neurons; vectorise only when the 302-neuron connectome demands it (Plan §42). |
| 2026-09-14 | 1 | World is a bounded box; walls are sensed and collide like obstacles. Obstacles are circles. | Simplest geometry with exact ray casts. |
| 2026-09-14 | 1 | Sensor reading = `1 - distance/range` clamped to [0,1]; rays at +45°, 0°, -45° from heading, range 4.0. | Proximity, not distance, so "closer" is "stronger" (Plan §8 uses the same convention for food). |
| 2026-09-14 | 1 | On collision the agent keeps its rotation but does not move; collision is counted. | Avoids tunnelling and gives a clean signal for later reward (Plan §9). |
| 2026-09-14 | 1 | Adapters are introduced now: `SensoryAdapter` (channel -> neuron, gain, tonic currents) and `MotorAdapter` (two neuron ids -> wheels). | Plan §2.2 wants independent layers from the start; both are tiny. |
| 2026-09-14 | 1 | Motor neurons run on a tonic current of 1.0 and sensors inhibit the *opposite* wheel. Front also excites the left wheel so a front+right conflict still turns right instead of freezing. | Hand tuning only (Plan §7); first wiring deadlocked and was fixed. |
| 2026-09-14 | 2 | Food is a gradient sense, not line-of-sight: signal = `1/distance` clamped to [0,1], routed to one of three sectors (front ±30°, left, right) by bearing. Food behind is still sensed on a side. | Plan §8 formula; chemosensation in the worm is not directional vision. |
| 2026-09-14 | 2 | Only the *nearest* food item is sensed, so the three food channels are mutually exclusive. | Binary neurons cannot compare magnitudes; two foods on opposite sides deadlocked the hand-wired brain. |
| 2026-09-14 | 2 | Hunger grows `hunger_rate` per tick, saturates at 1.0 (`World.starved`), resets to 0 on eating. Food is consumed on contact. Starvation has no consequence yet. | Consequence is a reward matter (stage 3). |
| 2026-09-14 | 2 | Hunger is exposed as an observation channel and gates food attraction in the demo through a HUNGRY interneuron. | Shows motivation observably: a sated agent drives past food. |
| 2026-09-14 | 2 | Shared `experiments/episode.py` holds the closed loop, trace record and rendering; stage demos only build scenarios. | Avoids copy-paste between stage demos. |
| 2026-09-14 | 3 | Reward lives in `learning/reward.py` as `RewardConfig` + `RewardTracker`; it takes plain event flags (ate, collided, starved) and position, not the World, so any environment can feed it. | Plan §9 single source of numbers; keeps learning decoupled from simple_2d. |
| 2026-09-14 | 3 | Exploration bonus counts distinct `floor(x/cell)`, `floor(y/cell)` cells with cell 1.0; collision penalty per tick, capped at 20 per episode; starvation scored once and ends the episode. | Plan §8-9 as revised. |
| 2026-09-14 | 3 | Episode ends inside `run_episode` on `world.starved` or after `steps`. Demo `hunger_rate` moved from 0.01 to 0.004 (starves in 250 ticks). | Plan §8 range 0.002-0.005; the stage-2 demo still eats both items and then starves, since respawn is stage 4. |
| 2026-09-14 | 4 | Genome = weight vector of the stage-2 topology (12 synapses) in insertion order; thresholds, decay and adapter wiring (tonic 1.0 on motors) stay fixed. Initial weights uniform in [-1, 1], mutation Gaussian σ=0.3, clipped to [-2, 2], truncation selection with 20% elites, no crossover. | Plan §10 algorithm literally; simplest thing that shows learning works. |
| 2026-09-14 | 4 | Fitness = mean total reward over 4 fixed train seeds (random start pose, 2 food items with seeded respawn, fixed obstacle). Held-out seeds 1000+ report generalisation. | Plan §10 revised: never one map. Obstacle layout stays fixed until stage 12. |
| 2026-09-14 | 4 | The closed loop moved from `experiments/episode.py` to `broomworm/episode.py` and scores ticks through a `TickScorer` protocol, so `learning` can import the loop without a cycle. | Layering: episode < learning < experiments. |
| 2026-09-14 | 4 | Brain JSON format v1: neurons (id, threshold, decay), synapses (source, target, weight, kind), meta (scenario, seeds, fitness...). `broomworm play` rebuilds adapters from `meta.scenario`. | Plan §40. Adapters are scenario code, not data, for now. |
| 2026-09-14 | 4 | Known failure mode of the evolved brain: on some seeds (e.g. 1000) it freezes when obstacle-right and food-right fire together and both motors are inhibited. | Binary neurons; addressed by graded activity from stage 5 (Plan §2.3), not by tweaking this stage. |
| 2026-09-14 | 5 | Dataset: Cook et al. 2019 hermaphrodite tables (SI 5 corrected July 2020, SI 4 cell lists) from wormwiring.org, extracted to CSV by `scripts/fetch_connectome.py`. No explicit license on the site; used as published SI for research, cited in `data/connectome/cook2019/README.md`. | Plan §11. |
| 2026-09-14 | 5 | 302 neurons = 83 sensory + 91 interneurons + 126 motor + CANL/CANR (type `neuron`, unclassified). Muscles, glia and end organs dropped. Dataset "weight" is EM serial sections of connectivity, not a synapse count. 38 chemical self-loops and 14 gap-junction self entries kept as in the source. | Keep the data as published; interpretation lives in `build_network`. |
| 2026-09-14 | 5 | Gap junction pair (a, b) becomes two positive electrical synapses a->b and b->a. Chemical sign: negative iff the source is one of the 26 classic GABA neurons (DD, VD, RME, RIS, AVL, DVB), else positive. Training may flip signs later. | Plan §11 revised. GABA-uptake-only neurons (Gendrel 2016) are not treated as inhibitory. |
| 2026-09-14 | 5 | Weight normalisation: per target, `w = sign * gain * weight / sum(abs incoming weights)`, so every neuron's absolute incoming weights sum to `gain` (default 1.0). | Bounded input per tick regardless of hub size; hubs and leaves get equal drive, which is a simplification to revisit at stage 6. |
| 2026-09-14 | 5 | Graded neuron mode added (`Neuron.graded`): `activity = clip(potential / threshold, 0, 1)`, no reset. Binary mode stays default for stages 0-4 code; connectome networks are graded. Brain JSON writes `graded` per neuron, format version unchanged (field optional, default false). | Plan §2.3 revised. |
| 2026-09-14 | 6 | Network stability: with graded neurons the per-hop steady-state gain is `gain/decay`. Measured: gain ≥ 1.0 (decay 0.5) drives all 302 neurons to an all-on state that never switches off; gain 0.5 barely reaches AVA. Defaults set to `gain=0.45, decay=0.5` (0.9 per hop): a stimulus spreads to ~250 neurons and fades to silence within ~70 ticks after it stops. | Almost all synapses are excitatory (only 26 GABA sources); recurrent excitation with gain ≥ 1 is a fixed point. Training will move weights, so this is an initialisation choice, not a law. |
| 2026-09-14 | 6 | Per-target normalisation dilutes strong inputs into hubs: ASHL→AVAL is 15 of AVAL's 894 incoming sections, so AVA responds weakly (peak ~0.04) while low-input targets of ASH (ASK, ADL, AIA) respond strongly (~0.2). Propagation is read as ranking and timing, not absolute level. | Known artefact; alternative normalisations tried (global scale with cap) were unstable. Revisit at stage 8 when motor readout needs AVA/AVB levels. |
| 2026-09-14 | 6 | `stimulate()` resets the network, holds the stimulus for N ticks, then runs M silent ticks. The propagation tree lists direct targets that activated strictly after their parent, ranked by peak. | Simplest observable of Plan §12. |
| 2026-09-14 | 6 | Debug screen (§41) is rendered offline from a trace recorded with `record_activity=True`: environment panel, bar chart of the top-N neurons by peak activity, text with sensors/motors/reward. GIF via matplotlib PillowWriter. Record now also snapshots food positions per tick. | Plan §41 revised: PNG/GIF, no live window. |
| 2026-09-14 | 7 | Sensory mapping lives in `connectome/mappings.py` as an explicit route list (`channel -> neuron, gain`) with JSON save/load; the Plan §13 table is `default_sensory_mapping()`. `SensoryAdapter` now accepts one channel driving many neurons. | Plan §13: explicit, configurable, documented. |
| 2026-09-14 | 7 | Obstacle left/right -> ALM + FLP of that side; front -> AVM + both FLP. Food left/right -> AWA, AWC, ASE of that side; front -> all six. Hunger -> NSM + ASI (positive gain, as the plan says). Danger channels -> ASH, unused until stage 14. | Left/right onto L/R pairs is an admitted artifice (worm steers by dorsal/ventral head bends). NSM/ASI biologically signal food presence, so a "satiety = 1 - hunger" variant may work better; the adapter can express it (tonic +1, gain -1) if stage 9 needs it. |
| 2026-09-14 | 7 | Sanity circuits confirmed in the loaded network: front touch (AVM/FLP) reaches AVA and AVD within 3 ticks; food smell (AWC) reaches AIY on the next tick. | Real touch-escape and chemotaxis pathways survive the normalisation. |
| 2026-09-14 | 8 | `MotorMapping` groups (Plan §14): forward = AVB, PVC, VB01-11, DB01-07; reversal = AVA, AVD, VA01-12, DA01-09; turn_left = SMDDL, SMDDR, RIVL; turn_right = SMDVL, SMDVR, RIVR. JSON save/load like the sensory mapping. | Dorsal read as left, ventral as right: admitted artifice, the worm bends dorso-ventrally. |
| 2026-09-14 | 8 | `GroupMotorAdapter`: drive = gain·(mean forward − mean reversal), turn = gain·(mean turn_right − mean turn_left), wheels = clip(drive ± turn) in [-1, 1]. Default gains 10 because untrained activity sits at 0.01–0.1. | Continuous wheels per Plan §14; negative drive is reversal. |
| 2026-09-14 | 8 | The world now accepts wheel commands in [-1, 1]; negative = backwards. Stage 1-4 binary brains are unaffected. | Reversal is the worm's main escape behaviour; a 2D car can drive backwards. |
| 2026-09-14 | 8 | `run_episode(brain_steps=K)` runs K brain ticks per environment step with the same input and hands the motor adapter the mean activity over the window (Plan §3.1). Default stays 1 for stages 0-4. | Averaging in the loop keeps adapters stateless. |
| 2026-09-14 | 8 | Untrained connectome: front touch gives reversal > forward (wheels ≈ −0.2), food drives SMD/RIV symmetrically, so there is no steering until weights are trained (stage 10). | Expected: SMD asymmetry is dorso-ventral, not left-right. |
| 2026-09-14 | 9 | `WormScenario`: connectome network (gain 0.45, decay 0.5) + default sensory/motor mappings, 5 brain ticks per env step, group motor gains 6. Hand-set inputs: tonic 0.1 on AVBL/AVBR and obstacle channel gain 3. World as in stage 4. Saved as brain JSON with `meta.scenario = "worm"` and `meta.params`; `broomworm play` rebuilds it. | Tonic AVB drive is biologically grounded (AVB are tonically active forward command neurons); without it activity is zero and the agent never moves. |
| 2026-09-14 | 9 | Result of the untrained worm (Plan §15 "does meaningful behaviour appear"): it drives forward (drive ≈ +0.33), front touch cuts drive to ≈ +0.19 and raises the reversal group tenfold, but the sign never flips because excitation also spreads into the forward group; food moves the SMD/RIV turn signal by ±0.005 only. So: sign-correct touch response, no reversal, no steering, many wall contacts. | The dataset carries no AVA/AVB mutual inhibition and no left/right steering asymmetry; both must come from trained weights (stage 10). Sensory gain has no effect beyond 1 because graded sensory neurons saturate at 1.0. |
| 2026-09-14 | 9 | Log format: JSONL per tick (`save_log`) with pose, sensors, hunger, wheels, reward, events and the top-N active neurons; plus PNG trajectory and debug GIF. | Plan §15 logging list. |
| 2026-09-14 | 10 | `Simulator` is now a NumPy matrix step (`weights[target, source] @ activity`) with identical semantics to the per-neuron `Neuron.integrate/update`; state is written back to the neuron objects every tick. Verified against a plain-Python reference on random mixed graded/binary networks. 5x faster (0.17 s per 300-step worm episode). | Plan §42 forbids GPU/Rust, not NumPy; stage-10 training was 44 min per modest run without it. |
| 2026-09-14 | 10 | Connectome training starts from the dataset weights: generation 0 = the stage-5 genome plus Gaussian perturbations (`seeded_population`), not uniform random genomes. Mutation sigma 0.02 absolute (weights: median 0.012, max 0.34), clipped to [-1, 1]; signs may flip (Plan §11). | Training in place keeps the biological initialisation meaningful; uniform random weights would erase the connectome. |
| 2026-09-14 | 10 | Per-generation metrics (best, mean) are written to a CSV next to the brain JSON (Plan §39). | Reproducible experiment records. |
| 2026-09-15 | 10 | First connectome training run (pop 40, 25 generations, 3 train seeds, 400 steps, sigma 0.02, ~10 min): best train fitness −18 → +56 (untrained −32). Held-out seeds 1000–1004: 0, 2, 4, 4, 4 food, zero collisions, vs untrained 0 food and 3–239 collisions. Strategy: forward drive with a right-biased curvature (mean wheels ≈ 0.8 / 0.6) that sweeps the map and bends toward food; reversal is essentially unused (0–2 ticks). | Plan §10 acceptance ("moves toward food, collides less") holds for the connectome with only weights trained. Whether this beats a random topology is stage 11. |
| 2026-09-15 | 11 | Control topologies (`connectome/variants.py`): random = same neurons and connection counts, uniform endpoints, permuted weights, same number of gap-junction self entries; shuffled = degree-preserving edge swaps (in/out degree per neuron for chemical, degree for electrical, 10 rounds x edges), permuted weights, data self-loops kept; dense = all 302x302 ordered pairs as chemical connections, the real count non-zero. | Plan §17 A/B/C. Dense with a 5% mutation mask is the Free mode of §38/§49: topology emerges from which weights become non-zero, same simulator and training. |
| 2026-09-15 | 11 | Every variant uses the same sensory/motor mappings (named neurons), tonic AVB, normalisation, world seeds and evolution budget; only the wiring differs. | Plan §49 "same neuron count, similar connection budget, same environment, same training budget". |
| 2026-09-15 | 11 | Comparison result and §17.1 decision are in `docs/results/a1/stage11_comparison_2026-09-15.md`: Real ~= Random on held-out fitness (29 vs 27), Real fastest sparse learner and the only collision-free variant, Shuffled and Free eat more. One run per variant; ordering not statistically established. Worm stays primary, controls become mandatory in later training runs. | Plan §17.1. |
| 2026-09-15 | 12 | Random maps (`environments/maze/generator.py`): 5 circular obstacles with radius 0.8-2.0, margin 1.0 from walls and each other, random clear start pose, 2 respawning food, all from one seed. `WormScenario(maps="random")`; `broomworm play --maps` overrides the brain's setting. | Plan §18: random start, food, obstacles. Circles only; rooms and doors are stage 15. |
| 2026-09-15 | 12 | Training from the dataset weights directly on random maps stalled (best −1.85 from generation 5 to 24, held-out food 1). The stage-10 brain trained on the fixed map generalises partly to random maps (7 food on 6 unseen maps, 2 maps with positive reward). Curriculum fixes it: continue evolution from the stage-10 brain (`--init-brain`). | Harder maps need a starting point; one evolution seed each, so this is an observation, not a law. |
| 2026-09-15 | 13 | Target ("come to X"): a `Target` in the world with the same 1/distance sector sensing as food, reached on contact, respawning from the world seed, reward +10 (`RewardConfig.target`). `WormScenario(task="target")` removes food and adds one target; hunger keeps ticking as time pressure. | Plan §19 revised. |
| 2026-09-15 | 13 | The default sensory mapping routes `target_*` to exactly the neurons of `food_*` (AWA, AWC, ASE per side), so a brain trained on food follows targets without retraining: the worm cannot tell the two apart. | Plan §19: "target becomes the new attractive signal". Making them distinguishable is a later, deliberate step. |
| 2026-09-15 | 14 | `Danger` zone (Plan §20): a circle sensed like food/target (1/distance sectors -> `danger_*` -> ASH), not solid; every tick inside costs `damage_rate` (0.25) health, health 0 is death and ends the episode. Reward: damage −2 per tick, death −20 once (separate from starvation). For the robot this is a stairwell or a forbidden zone. | ENEMY of the Doom track is the same object under another name. |
| 2026-09-15 | 14 | Danger zones are placed clear of obstacles, agent and target; `WormScenario(dangers=N)`; stage-14 task = target + 1 danger on random maps, trained by curriculum from the random-map brain. | One new complexity on top of stage 13. |
| 2026-09-15 | 12-13 | Evaluation on six unseen random maps (`docs/results/a1/stage12_13_eval_2026-09-15.md`): the random-map brain eats ~1 food per map with zero collisions; the fixed-map brain eats slightly more but collides. Targets transfer without retraining (3 of 6 maps for the stage-10 brain) because they drive the same neurons as food; the target task is harder than food because a single target does not reset hunger, so every episode is capped at 250 ticks. | Plan §18 acceptance met weakly; §19 transfer shown. Both effects are one-seed observations. |
| 2026-09-15 | 14 | Result: curriculum from the stage-12 brain on target + 1 danger gives zero damage and zero deaths on six unseen maps (baseline: 14 damage ticks, 3 deaths) but 208 collisions and only 2 targets. Reward −21 for both. | Avoidance is cheap for the network (ASH -> AVA reversal exists); approach is hard because a single target is a weak gradient and there is no food to reset hunger. Consider a longer episode cap or a target-reached hunger reset for the robot's "call" task. |
| 2026-09-15 | 15 | Apartment maps: 2x2 rooms, 0.4 walls, 2.4 doors at random positions >= 1.2 from corners, one 1-3 unit furniture rectangle per room, agent and food placed with clearance 1.0. Rectangular walls use a slab ray test and circle-rectangle collision; `World.clearance()` is now the single placement check. | Plan §20.2. |
| 2026-09-15 | 15 | Result: the stage-12 brain passes a door on 2 of 6 unseen apartments; 25 generations of curriculum on apartments stalled at −2 and produced a brain that moves less and eats nothing. The worm with three ideal rays and sigma-0.02 evolution has hit a ceiling on structured maps. | Feeds the two-track restructuring: platform (real vacuum sensors, benchmark) first, then a bake-off of brains including a classical controller as the floor. |
| 2026-09-15 | 16 | Vacuum task (`task="clean"`): `DirtMap` of 1x1 cells whose centre is not inside a solid; cleaning radius = body radius + half a cell (the brush is wider than the body); coverage = cleaned / floor. Battery = 1 − hunger with drain 0.002 per tick (500 ticks); `Dock` is persistent, charges 0.02 per tick while the agent is within 0.6, beacon range 8. The agent starts on the dock. Reward: +0.1 per cleaned cell, +10 per docking with battery ≤ 0.3, discharge = starvation (−20, episode ends). | Plan §20.3 stage 16. |
| 2026-09-15 | 16 | The dock beacon is routed to the food neurons, so a food-trained worm treats the dock as food and keeps returning to it (11 dockings, 4% coverage on an unseen apartment). Coverage needs an explicit drive (coverage planner, stage 19) and arbitration (stage 20). | Expected; recorded as the stage-16 baseline. |
| 2026-09-15 | 17 | `SensorSuite` (environments/sensors.py) wraps the world: N rays with relative gaussian noise, dropout and a delay buffer; bumper (side by nearest front ray), two cliff sensors probing 0.7 ahead at +-30 degrees, right-side wall IR (range 1.0), dead-reckoning odometry with multiplicative noise and a random-walk gyro bias. Legacy `sensor_left/front/right` are the strongest ray per sector so every mapping keeps working; gradient and internal-state channels pass through untouched. Presets `ideal`, `vacuum`, `noisy`; the scenario builds a fresh seeded suite per episode. | Plan §20.3 stage 17. Units stay abstract (world units), to be scaled when hardware is chosen. |
| 2026-09-15 | 17 | The stage-12 brain loses most of its collision-free driving under the vacuum preset (0 -> 171 collisions), less under the noisier one (104): ray geometry and latency matter more than noise amplitude. | Train on the target preset; ideal is a reference only. |
| 2026-09-15 | 18 | `Brain` protocol (`candidates/base.py`): `reset()` and `act(channels) -> (left, right)`; `WormBrain` wraps network + mappings + brain ticks and saves/loads the Plan §40 JSON with scenario params; `ScriptedBrain` wraps a function. `run_brain_episode` is the loop for any brain; the old `run_episode` stays for the stage 0-17 code. World factories moved to `broomworm/worlds.py` so the benchmark does not import experiments. | Plan §3.3. |
| 2026-09-15 | 18 | `BroomwormEnv` (gymnasium): observation = fixed-order channel vector of the chosen sensor preset (odometry unbounded, the rest in [0, 1]), action = two wheels in [-1, 1], terminated on death, truncated at the step cap, seeded by `reset(seed=)`. | Lets stable-baselines3 / any RL library run on the same world (Plan §20.4 candidate 5). |
| 2026-09-15 | 18 | `BenchmarkConfig` defaults: apartment maps, clean task, vacuum sensors, 6 unseen maps (3000-3005) x 3 sensor-noise repeats, 800 steps. Leaderboard sorted by mean reward with std; results as JSON + CSV per brain in `runs/benchmark/` (copy to `docs/results/` when publishing). | Plan §3.3: no brain is compared outside the benchmark. |
| 2026-09-15 | 19 | `layer/occupancy.py`: log-odds grid (cell 0.5, hit +0.9, miss −0.6, clamp ±5) updated from odometry pose + ray proximities (no hit = free along the full range), robot footprint marked free. `layer/path.py`: 4-connected BFS treating unknown as traversable with a 1-cell clearance; `nearest_unswept` sends the robot to the closest unswept or unknown cell (exploration falls out of coverage). | Plan §3.2 / §20.3 stage 19: classical, no learning. |
| 2026-09-15 | 19 | `PlannerLayer` wraps any Brain: maps from channels, replans every 5 ticks, injects the waypoint bearing as `target_left/front/right` with strength 0.6 (overriding real target channels). Modes: coverage, goal, passthrough. `GradientFollower` is a scripted test driver for the layer, not a candidate. The ideal preset now includes bumper, cliff and wall sensors with exact odometry (ideal = noiseless, not sensorless). | The inner brain never sees the map; hybrids of Plan §20.4 are this wrapper around a learned brain. |
| 2026-09-15 | 19 | Benchmark (apartment/clean/vacuum, 18 episodes): worm 7% -> 14% coverage with the planner, follower 12% -> 24%; both discharge before the 800-step cap because nothing sends them to the dock. | Motivation for stage 20 arbitration. Results in `docs/results/a1/benchmark_2026-09-15/`. |
| 2026-09-15 | 20 | `NeedsArbiter` (layer/needs.py): battery ≤ 0.4 -> go to the dock and stay until ≥ 0.95 (hysteresis); pending call -> go there (done within 1.0); else coverage. Dock = pose at the first tick in the odometry frame. `PlannerLayer(mode="needs")` parks the wheels while charging on the dock; bumper hits are written into the map as certain obstacles; the test driver escalates its escape (back off, then turn longer each bump). A pending call survives `reset()`. | Plan §20.3 stage 20. The arbiter is a state machine outside the brain, as planned. |
| 2026-09-15 | handoff | Trained brains and benchmark results are copied into `docs/brains/a1/` and `docs/results/a1/benchmark_2026-09-15/` so a fresh clone can replay and benchmark without retraining (`runs/` is git-ignored). | Repository moves to another machine. |
| 2026-09-15 | 20 | `broomworm benchmark --driver` runs the scripted `GradientFollower` (row `driver_follower`), so the stage-19/20 driver rows are reproducible from the CLI; a planner-wrapped row is named `+planner` for coverage mode and `+<mode>` otherwise (`+needs`). | The driver rows had been produced outside the CLI; one suffix for every mode would overwrite the coverage row. |
| 2026-09-15 | 20 | Bug fixed in `run_benchmark`: the `ideal` preset skipped the `SensorSuite`, so benchmark "ideal" meant raw world channels without odometry or bumper and any `PlannerLayer` brain crashed. Every preset now goes through the suite (ideal = noiseless, as stage 19 states). Published rows are unaffected (none used ideal). | Stage 19 assumption vs. code. |
| 2026-09-15 | 20 | Benchmark result (`docs/results/a1/stage20_needs_eval_2026-09-15.md`): needs mode halves the driver's collisions (25 -> 12) but nobody survives on the vacuum preset (driver 0/18, worm 2/18); on the ideal preset the same driver survives 15/18 with coverage 0.28 and 3 dockings. The arbiter works; the dock pose in the odometry frame drifts (`odom_sigma` 0.05, gyro 0.002 rad/tick) and the robot never re-finds the dock. | Stage 20 is not closed on the benchmark preset. Candidate fix, not implemented: let the planner home on the existing `dock_*` beacon channels during the final approach and re-anchor the odometry frame on the dock at every docking. |
| 2026-09-15 | 20 | `SensorSuite.reset` pre-fills the delay buffer with the reading at rest, so the first `delay` ticks report stale values instead of zeros. | A robot that has been sitting on its dock knows where it is; zero odometry at tick 0 made the arbiter store the dock at (0, 0) and every vacuum-preset episode discharge. |
| 2026-09-15 | 20 | Dock homing: heading for the dock, once the beacon (`dock_*`, exact bearing, range 8) is >= 0.5 (within 2 units) the planner layer injects the beacon sectors instead of the path waypoint; a rising battery means "on the dock" (parks the wheels, re-anchors the dock estimate to the current odometry pose). With no path on the map the layer heads straight for the goal. Threshold 0.5 rather than "in range": at 8 units the beacon points through walls and the robot got stuck on the wall. | Dead reckoning drifts ~1 unit per 300 ticks; a real dock has an IR beacon and a real robot a charging flag. Every trip with beacon homing docked. |
| 2026-09-15 | 20 | The arbiter leaves for the dock when `battery <= low + trip`, where the layer estimates `trip = cost_per_cell (0.008) * path cells to the dock` (beeline cells when no path). `low` 0.4 stays as the floor. | A fixed 0.4 (200 ticks) is enough from the next room and not from the far one (trips measured at 150-180 ticks over ~40 cells). Still a plain state machine outside the brain. |
| 2026-09-15 | 20 | Remaining failures (2/18 driver, 3/18 worm) are map drift: the grid is in the odometry frame, so a door re-mapped after drift shifts by ~1 unit against a 2.4-unit opening and the path hits the jamb. No scan matching / loop closure is added at this stage. | Plan §51 minimal implementation; the acceptance (cleans, recharges when needed, comes on call on unseen apartments) holds for 16/18 episodes. SLAM correction is a platform item for A2/A3 if a candidate needs it. |
| 2026-09-15 | A1 | Phase A1 closed: apartment with dirt, battery and dock; vacuum sensors with noise; Brain interface, Gymnasium env, benchmark; map, coverage planner, needs arbitration; the worm passes the benchmark (Plan §47). All six leaderboard rows were re-run on the final code because the delay-buffer change alters the vacuum preset. | Milestone record. |
| 2026-09-15 | 21.1 | `RoombaBrain` (candidates/roomba.py): spiral -> escape (back off 3 ticks, spin away from the contact side 6-15 ticks, longer with every bump) -> wall-follow on the right IR for 120 ticks -> spiral; battery <= 0.4: drive at the dock beacon sector, wall-follow while no beacon; battery rising = parked until 0.95. No map, no odometry, no learning. Benchmarked bare (its docking is its own), not under the needs layer. | Plan §20.4 candidate 1, the floor every trained brain must beat. Parameters hand-set once, not tuned on the test maps. |
| 2026-09-15 | 21.1 | Floor on the benchmark: reward −0.06 ± 11.0, coverage 0.19, 18 collisions, 4 dockings, survives 13/18. Failures are the same class as stage 20: battery low while out of beacon range (8 units) in another room, and wall-following does not find the dock in 200 ticks. | Recorded as the A2 baseline row. |
| 2026-09-15 | 21.1 | `broomworm benchmark --scripted follower|roomba` replaces the stage-20 `--driver` flag. | One switch for every hand-written brain. |
| 2026-09-15 | 21.2 | Training harness (`learning/bakeoff.py`): a candidate is any Brain that is also `Trainable` (flat weight vector); fitness = mean benchmark reward on train maps 100-102 (apartment / clean / vacuum, 800 steps, one noise repeat) with the brain wrapped in the same planner layer the benchmark applies (`--layer needs`); search = stage-10 evolution (pop 40, 25 gens, sigma 0.02, weights in [-1, 1], dense with 5% mutation mask). Fitness of a generation is evaluated by a process pool; each worker builds the candidate once. `broomworm evolve --candidate ...` saves brain JSON + CSV + meta. | Plan §3.3 "reward is the single source of numbers" and §49 "same environment, same training budget": one code path for the worm, its controls and (21.3) a network from scratch. |
| 2026-09-15 | 21.2 | `WormBrain.from_file` now uses the saved network itself instead of copying weights into the connectome template, so control topologies round-trip through JSON; the scenario supplies only the named-neuron adapters. | Needed for saved controls; behaviour for the real connectome is unchanged. |
| 2026-09-15 | 21.2 | Bake-off protocol for the worm family: every topology trains from its untrained (dataset-derived) weights with the same budget; the real worm additionally trains from the stage-12 food brain as a separate "curriculum" row, since no trained control brains from stage 11 were saved. | Fairness (§49) plus the stage-12 observation that harder tasks need a starting point. |
| 2026-09-15 | 21.3 | `RNNBrain` (candidates/rnn.py): 32 hidden units with the worm's graded leaky dynamics (decay 0.5, activity clipped to [0, 1]), dense input -> hidden -> hidden, linear readout clipped to two wheels, 5 ticks per env step, inputs = every bounded channel of the sensor preset (odometry excluded), weights N(0, 0.1) at birth, ~2.5k weights vs the worm's 5.9k. Own JSON format (`kind: rnn`); `load_candidate` dispatches by file. | Plan §20.4 candidate 4: "small recurrent net, same evolution, honest comparison". Kept smaller than the worm so evolution with sigma 0.02 can move it; the 302-unit free topology is already the dense control. |
| 2026-09-15 | 21.4 | `PlannerWrapper` (gym_env.py) puts the map + planner + needs layer under a Gymnasium policy: the observation carries the planner's virtual `target_*` gradient and the action is overridden to stopped wheels while parked on the dock, exactly what a wrapped Brain gets. `BroomwormEnv` now always builds the sensor suite (ideal = noiseless), the same fix as the benchmark's. | An RL policy must be compared under the same engineered layer (Plan §3.2, §20.4). |
| 2026-09-15 | 21.4 | PPO candidate (`learning/rl.py`, optional `rl` group: torch + stable-baselines3, CPU): MlpPolicy on the bounded channels (odometry excluded), n_steps 2048, batch 64, training maps 100-102 round-robin, deterministic actions at test time. Saved as a JSON descriptor next to the SB3 zip (`kind: ppo`); `load_candidate` dispatches. CI installs core + dev only, so the rl test is skipped there. | Plan §20.4 candidate 5: another class of algorithm, budget expressed in environment steps rather than generations, recorded per run. |
| 2026-09-15 | 21.5 | NCP candidate (`candidates/ncp.py`, `ncps` library in the rl group): `AutoNCP(32 units, 2 motor outputs, seed)` wiring over a CfC cell, inputs = bounded channels, one CfC step per environment step, torch parameters exposed as the flat weight vector and trained by the same evolution as the worm and the RNN (not by gradient, so the training budget stays comparable). Saved as `kind: ncp` JSON with the wiring seed. | Plan §20.4 candidate 6: the published worm-inspired architecture under the same protocol. Gradient training would need a differentiable objective or PPO with a custom recurrent policy; noted as a possible later variant, not done. |
| 2026-09-15 | 21 | Reward v2 (`RewardConfig`): the docking bonus (+10 at battery <= 0.3) is paid once per discharge cycle, re-armed only after the battery has been >= 0.9; a cleaned cell is worth 0.5 (was 0.1), a fifth of a food item. Every A2 candidate is retrained and every published benchmark row re-run under v2; rows before this entry are reward v1. | The evolved RNN found the hole: 6.6 dockings per episode by jittering at the dock edge, coverage 0.04, reward 6.7. Cleaning must be the dominant term for a vacuum (owner's call: "cleaning is the food"); the virtual dirt gradient already reaches the brain through the planner layer, so only the scale changed. |
| 2026-09-15 | 21.7 | Seed-0 bake-off (reward v2, `docs/results/a2/a2_bakeoff_2026-09-15.md`): PPO 49.7 > Roomba floor 29.5 > worm-from-stage-12 26.5 > dense 17.7 > random 12.5 > worm 9.9 > rnn 0.2 > shuffled −1.3 > ncp −6.2; the scripted driver under the same layer scores 55.6. Real ≈ Random, Real < Free, Real < PPO; the real connectome is ahead on no axis. Provisional decision per Plan §17.1: A3 stack = engineered layer + a learned reactive brain, choice of brain deferred to seeds 1-2. | One evolution seed; rnn/ncp initialisation (N(0, 0.1), sigma 0.02) leaves their wheels near zero, to be corrected before judging them. |
| 2026-09-15 | 21.7 | The dense (free) brain JSON is 11 MB (91k synapses) and is not committed; its curve and meta are. | Pre-commit large-file limit 1 MB; reproducible in ~40 min. |
| 2026-09-15 | A2 | Three-seed bake-off closed: PPO 55.8 ± 11.6 above the Roomba floor 29.5 on every seed; worm with curriculum 27.5 ± 0.7; budget-matched worm family real 10.0 ± 1.2 ≈ random 9.8 ± 5.9 ≈ shuffled 15.7 ± 13.3 ≈ dense 12.3 ± 7.3. Decision for A3: engineered layer + PPO reactive brain; the connectome stays the primary control. First A3 task: PPO's survival (0.63) via a survival term or beacon-docking override. | Plan §17.1 clause ("Real ahead on no axis -> switch the robot brain, keep the worm as control"), applied with the Plan's >= 3 seeds. |
| 2026-09-15 | 21.8 | `PlannerLayer(dock_autopilot=True)` (default): in needs mode, while the arbiter's state is "charge", the layer's own `GradientFollower` drives on the merged channels (path waypoint, then beacon) instead of the inner brain; parking while charging is unchanged. The brain is consulted only for cleaning and calls. | PPO seed 0 died in 9/18 episodes on dock trips that started 5-6 units from the dock: it never came within 3 units and hit the bumper up to 91 times per trip; it learned a sweeping style, not gradient following. Return to dock is a safety routine of a real robot (Roomba's is scripted too), so it belongs to the engineered layer (Plan §3.2). No candidate is retrained; only the dock trips change. |
| 2026-09-15 | 21.8 | Final A2 table (dock autopilot, 3 seeds): PPO 61.8 ± 4.4 (survival 0.93, coverage 0.31) > driver 55.6 > worm-curriculum 30.0 ± 3.0 ≈ Roomba 29.5 > shuffled 27.1 ± 8.9 > real 20.8 ± 2.6 ≈ dense 20.3 ± 1.5 > random 12.2 ± 3.7. Plan §50: Real > Random, Real ≈ Free, Real < PPO. A3 decision confirmed: engineered layer + PPO; remaining hardware concern is PPO's collisions (20 per episode). | Same brains as before, only the dock trips changed; survival 0.61-0.76 -> 0.93-0.98 across the board, which is what let the worm-family ordering emerge from under the death penalty. |
| 2026-09-15 | 21.9 | `PlannerLayer(bumper_reflex=True)` (default): on bumper/cliff contact the layer backs off 3 ticks and spins away 4-10 ticks (escalating with repeated bumps) before consulting the brain; skipped while the dock autopilot drives (it has its own escape). | Owner's call to cut PPO's collisions in the layer rather than the reward; the same reflex Roomba and the scripted driver already had. |
| 2026-09-15 | 21.9 | Mapping fix: `PlannerLayer(footprint=0.5)` marks only the body free on the map each tick; before, the brush radius 1.0 was used and walls the robot drove up to (closer than 1.0) were erased, so paths ran through them. `brush` still defines the swept cells. | Found by the reflex test on map 3001: a dock trip with a path through a wall at (10.2, 19.2) whose cells read −5.0 (free). This, not odometry drift, was the main "map drift" failure of stage 20. |
| 2026-09-15 | 21.9 | Final A2 table (reflex + footprint fix, 3 seeds): PPO 61.9 ± 0.2 (8 collisions, survival 0.93) > driver 56.7 > worm-curriculum 44.0 ± 7.6 > shuffled 40.4 ± 9.7 > real 32.1 ± 5.8 > Roomba 29.5 > dense 26.2 ± 3.1 > random 15.9 ± 4.1. Plan §50: Real > Random, Real ≳ Free, Real < PPO. A3 decision: engineered layer + PPO; curriculum worm is the torch-free fallback. | Same brains, only the layer changed; the connectome's advantage over random wiring appeared once docking and mapping defects stopped dominating the score. |
| 2026-09-15 | 21.10 | rnn and ncp readouts get a trainable forward bias (0.5, 0.5), the rnn readout is scaled by 1/sqrt(hidden), the rnn hidden init scale is 0.3. Genome length +2 for both; old rnn/ncp JSONs are incompatible (none published before). | Untrained rnn drove backwards or spun, untrained ncp stood still, so evolution saw no cleaning reward; the worm has the same "tonic forward drive" through AVB. Measured before/after with a 150-tick probe on map 100. |
| 2026-09-15 | 21.10 | Final A2 table with rnn 34.5 ± 6.4 and ncp 28.8 ± 2.8 (3 seeds): Real ≈ small RNN, Real > NCP under the same evolution. A3 decision unchanged. | `docs/results/a2/a2_bakeoff_2026-09-15.md`. |
| 2026-09-15 | 22.1 | `RobotLink` (hardware/link.py): `reset() -> frame`, `step(left, right) -> frame`, `truth()`; `SimLink` is the world + sensor suite behind it (same warm start and ordering as `run_brain_episode`, verified by test), `LineLink` a machine behind a JSON-lines text stream (TCP via `connect_tcp`, or any text file: a serial port needs no new dependency). `drive()` is the one closed loop for both; it records a JSONL log (header meta, then per tick: wheels, channels, raw reading, true pose when known). | Plan §20.5 "only Environment and adapters change": the brain and the planner layer never learn which side of the link they are on. No hardware in hand, so the machine side is a reference `FakeRobot` that serves the protocol from the simulator; the firmware imitates it. |
| 2026-09-15 | 22.1 | The wire carries physical units (`RawReading`: metres, radians, SoC fraction, beacon sectors; `null` = no echo). `Calibration` converts to the suite's channels, same keys and order; `unit_m` 0.33 (body radius 0.5 u = 0.165 m) and `tick_s` 0.1 are assumed, not measured, and live only in the calibration. Legacy `sensor_left/front/right` are rebuilt from the rays the same way the suite does. | Keeps the firmware dumb and the units auditable (`docs/hardware.md`); the implied 0.66 m/s top speed is above a real vacuum's, so the tick or the scale will move once the drive is measured. |
| 2026-09-15 | 22.1b | Owner's decision: phase A3 runs on an ACEBOTT QD001 + QD003 kit car with one or two basic Arduino-kit sensors, not on a vacuum (Plan §20.5.1). Preset `car`: 3 rays from one ultrasonic on a servo (`sweep`: one ray refreshed per tick, others hold), `proximity_bumper` 0.7 u from the front rays instead of a bumper (fires before contact, blind to side hits), line module as cliff sensors, right IR obstacle module as a binary wall sensor (0.75 u), odometry integrated from the commanded wheels (`odom_source="commands"`, 10 % noise, blind to slip and to pushing a wall), no gyro (`gyro_heading` = odometry), dock beacon = a marker seen by the K210 within +-30 degrees and 5 u. Scale 0.2 m/u, 0.4 m/s top speed, assumed. | Plan §2.4: the simulator must not be more convenient than the machine. The stock ultrasonic being on a servo is unverified; if it is fixed the preset drops to one ray. |
| 2026-09-15 | 22.1b | The vacuum preset's random stream is untouched by the car changes (the sweep warm-up runs only for sweep presets), so every published vacuum row still reproduces (`tests/test_roomba.py` caught the regression). | Reproducibility of `docs/results/` rows. |
| 2026-09-15 | 22.1b | `LineLink` integrates the commanded wheels into odometry when the machine reports `odom_m: null` (no encoders); `Calibration.for_preset` carries the scale per platform; the firmware stays dumb. | Same dead reckoning as the preset, one implementation on the host. |
| 2026-09-15 | 22.1b | A2 brains on the `car` preset without retraining (18 episodes each, seed-0 brains): worm-curriculum 41.3 ± 17.6 > worm 38.8 > driver 34.9 > PPO 31.9 (52 collisions) > rnn 26.7 > Roomba 24.6; survival 0.5-0.9 (was 0.93-0.98 on vacuum). PPO and rnn read their two missing rays as zeros. The worm family transfers best because its sensory mapping uses the sector channels, which the car still has. | Dock trips fail more with command odometry and a 60-degree beacon; retraining on the preset is the A2 protocol for the new platform (22.1c). |
| 2026-09-15 | 22.1c | Three-seed car table (`docs/results/a3/stage22_car_2026-09-15.md`): PPO retrained on the car preset 43.0 ± 4.0 ties the vacuum-trained curriculum worm used as is (41.2 ± 0.2); the worm has half the collisions (14 vs 31). Retraining the worm on the car preset lowers it to 30.1 (train fitness 68: evolution fitted the three training maps). Transferred PPO 27.3 with 63 collisions. Decision: the car drives with the transferred curriculum worm, PPO-car is the second brain to try on the machine; revisited after the first real logs. | Tie on reward, tie-break on collisions (no bumper on the car) and deployability (no torch). Plan §17.1 rule with >= 3 seeds. Retrained brains and every row published under `docs/brains/car/` and `docs/results/a3/benchmark_car_2026-09-15/`. |
| 2026-09-16 | 22.1d | Day-one tooling built against the simulator link so it is tested before the car exists: `selftest` (frame format, dropouts and round trip at rest, each wheel pair alone must turn the odometry heading the right way, forward must advance it), `calibrate` (unit_m = measured metres / (ticks * speed), wheel_base = 2 * speed * ticks / measured radians, tick = measured round trip; saved to JSON and loaded by `--calibration`), room files in metres converted with `unit_m` (`hardware/room.py`; the marker becomes the dock, the floor gets the dirt map), `plot-log`. On the simulator the measured tick is the loop's own time, meaningless; only the tcp link gives a real one. | Plan §20.5 order: manual control and recording, then the comparison, and both need the scale and the room. |
| 2026-09-16 | 22.1e | The camera beacon in the `car` preset now needs line of sight (`_camera_beacon` checks `ray_distance` against the marker distance); before, it saw through walls, which made the simulator more convenient than the machine (Plan §2.4). | Found while diagnosing failed dock trips. |
| 2026-09-16 | 22.1e | Failed dock trips on the car preset are the second and third trips: dead reckoning drifts 2-8 u, the car gets within 2-4 u of the dock with the marker outside the 60-degree view, and circles until flat. Layer v2 (camera presets only): homing on any sighting, dock estimate re-anchored from every sighting (distance = 1/strength, bearing from the sector), spin search (full turn on the odometry heading, 8 ticks forward, repeat) near the estimated dock without a sighting; the bumper reflex runs during the search. Vacuum behaviour is byte-identical (the Roomba and autopilot tests reproduce). | Survival driver 0.61 -> 0.89, PPO-car 0.81 -> 0.89, curriculum worm 0.80 -> 0.94 (18/18 in the diagnostic), rewards up 1-12 points; decision for the car unchanged. Stall detection and scan matching were not needed for this; recorded as the next candidates if real odometry is worse than modelled. |
| 2026-09-16 | 23.1 | Robustness sweep (`learning/robustness.py`, `broomworm robustness`): one preset parameter at a time, values worse than assumed, the benchmark on 6 maps x 2 noise repeats per cell, brains of seeds 0-2; `BenchmarkConfig.sensor_config` overrides the preset name. The layer is rebuilt per cell so its camera field of view follows the swept value. | Plan §20.7 axis C5. Result: worst-case drop −30 % (worm, marker range 2 u) vs −39 % (PPO, delay 3); the worm is calmer under noise amplitude (−2 vs −10) and latency (−11 vs −19), PPO under odometry drift (−6 vs −12) and a narrower camera (+1 vs −5). Verdict tie. |
| 2026-09-16 | 23.2 | `TrainConfig.train_repeats` (noise seeds per training map, `--train-repeats`). Retraining on 12 maps x 2 repeats: the curriculum worm 44.4 ± 5.6 (3 maps: 30.1; transferred: 42.1), PPO at the same 300k steps 37.5 ± 8.4 (3 maps: 46.5; seed 0 learned to jitter at the marker: 7 dockings, coverage 0.14). Budget accounting: the evolution consumes 24 000 episodes (19 M env steps) against PPO's 375 episodes. | Plan §20.7 axis C6. The A2 protocol matches budgets per method (generations / steps), not per experience; both numbers go into the map. |
| 2026-09-16 | 23.2 | 13 evolution workers plus a sweep exhausted the Mac's memory; the OS killed the chain after seed 1. Use <= 6 workers when anything else runs. | Operational. |
| 2026-09-15 | 22.1 | `Teleop` reads `w a s d x` or `left right` pairs line by line from any text stream (blank = repeat, `q` = stop); on a tty `broomworm drive --teleop` switches it to cbreak key mode. `compare-log` replays the recorded wheels in the simulator (map from the log's `seed`, or `--seed`) and reports per-channel RMSE / max error, bumper agreement, final true-pose distance. Same preset, same seeds: zero error (test + demo); the ideal preset against a vacuum log shows the noise and the 3-vs-5 ray geometry, so the tool is for same-preset comparisons. | Plan §20.5 order: manual control and recording, then the comparison against the simulator, before any autonomy on the machine. |
| 2026-09-16 | B1 | Mini-Doom lives inside the existing 2D `World` (no new simulator): `Enemy` (mutable: position, health in hits, walk speed, attack range 3 u, damage 0.05 health per tick in range with line of sight), the exit = a `Target` with `exit_ends=True` (reached -> `exited`, `finished`), the gun = `World.step(..., fire=True)`: a hitscan along the heading that hits the nearest visible body (radius 0.6 u, a Doom zombie's 20 map units) within `fire_range` 6 u, `fire_cooldown` 5 ticks, three hits kill. Walking enemies close in while they see the agent and stop at half their reach; enemies are not solid. Episodes end on `world.finished` (dead or exited); loops before B1 only ended on death. | Plan §21-23, §29-31 with the least new code; Plan §2.4: the gun cone is the body, not the 30-degree sector, because Doom's pistol is a hitscan and a wider cone would make the simulator more convenient than the game. First version had the 30-degree cone; replaced before any brain was trained. |
| 2026-09-16 | B1 | Sensing: an enemy is a *seen* danger: it appears on the `danger_*` sectors only with line of sight (danger zones stay a smell through walls); new channels `ammo` (1 while the gun is loaded) and `aim` (1 - |bearing| / 30 degrees of the nearest visible enemy inside the front sector, 0 otherwise): the one structured number a shooter needs to centre the gun line without a framebuffer. Both pass through the sensor suite. Default worm mapping: `aim` -> ADLL, ADLR; the trigger reads the pharyngeal pumping motor neurons M3L, M3R, M4, MCL, MCR (`MotorMapping.fire`, `FireAdapter`, gain 10): "bite" as fire, an admitted artifice per Plan §22; the topology is untouched. | Plan §25 (structured observation), §22 (an artificial motor mapping is fine, the topology is not changed for FIRE). Old brains reload with the trigger attached and behave identically on the old tasks (the fire group is only read). |
| 2026-09-16 | B1 | The trigger is an optional attribute: a brain sets `fire` in [0, 1] after `act`; the loop pulls the trigger above 0.5 (`episode.fire_of`). rnn / ncp get a third readout unit on the doom task (`outputs=3`, old JSONs load as 2), PPO a third action (`BroomwormEnv(fire=True)`, Box(3)); the planner layer passes the inner brain's trigger through. Reward: +2 per hit, +10 per kill (`RewardConfig.hit`, `.kill`), the exit pays the target bonus (+10), damage and death as before. | Plan §23 reward list; keeps `Brain.act -> wheels` for every stage 0-22 brain. |
| 2026-09-16 | B1 | Levels (`environments/doom/levels.py`), every layout a function of the seed: `doom1` empty room + exit >= 8 u away; `doom2` a partition with one 3-u doorway plus three round obstacles, player and exit on opposite sides; `doom3` = 2 + one stationary enemy, no gun (avoid); `doom4` = 3 with the gun; `doom5` the enemy walks at 0.1 u/tick (half the player); `doom6` an apartment (stage-15 generator, no food) with two walking enemies and the gun. No hunger (`hunger_rate` 0): the episode cap (600 steps) is the time pressure. Maps `doom<n>` require task `doom`. | Plan §27-32 by rising difficulty on the platform that exists; Plan §51.12 one new complexity per level. |
| 2026-09-16 | B1 | `DoomguyBrain`, the zero-learning floor: escalating escape on contact, about-face and run when hit without a gun, with a gun turn toward the enemy and nudge the heading until `aim` >= 0.8, then stand and fire when in range; after two bumps it stops chasing (no path planning) and only shoots what is in range; otherwise the exit gradient with wall veering. On the simulator (6 unseen maps, 600 steps): exits 100 % on level 1, 83 % on 2 and 5-6, 33 % on 3-4; kills 0.83-1.33 per episode where it has the gun; never dies. | The floor every trained brain must beat (Plan §20.4). Its avoidance without a gun is weak by design; it is a floor, not a candidate. |
| 2026-09-16 | B1 | The magazine is finite: the gun arrives on level 4 with 50 rounds, the Doom pistol's, and the `ammo` channel is the fraction left (1 at the start, 0 when empty); an empty gun does not fire. The engine world reports the same fraction from `AMMO2`. | Plan §2.4. The first B2 pass had free ammo in the simulator while the engine had 50 rounds, and the evolved worms held the trigger (50-110 shots per episode for 0.3-0.7 hits): harmless for the reward (a hit is +2 against a ±30 range) but a simulator convenience the machine does not have, and it would have made the simulator-vs-engine row meaningless. Those numbers were discarded, not published; the lanes were re-run. |
| 2026-09-16 | B2 | Protocol = A2's with the planner removed (`--layer none`: the exit is a global gradient, a map adds nothing on Doom v1): ideal sensors, 600 steps, training maps 100-102, evolution pop 40 x 25 gen sigma 0.02, PPO 300k steps; three seeds; trained on `doom4` and `doom6`, benchmarked on all six levels (12 unseen maps 3000-3011, one repeat: the ideal preset is noiseless). Rows: worm, curriculum worm (from the stage-12 food brain), worm_random, worm_shuffled, rnn, ncp, PPO, plus the floors and the A2 brains without retraining (transfer, axis C3 on Doom). | Plan §20.4, §20.7 axis C9; budgets matched per method as in A2 so the Doom row is comparable with C1. |
| 2026-09-16 | B4 | Optional `doom` dependency group: `vizdoom` 1.3.0 (a cp314 macOS-arm64 wheel exists, no source build, Plan §24 check done); the engine runs headless (`set_window_visible(False)`, 160x120, no sound, PLAYER mode, skill 3, `set_seed`). One engine per process: building a new `VizdoomWorld` closes the previous one (init 0.2 s after the first launch; 600 tics in well under a second). | Plan §24; project rule that torch-like candidates live in an optional uv group; CI keeps core + dev. |
| 2026-09-16 | B4 | Maps are written by `environments/doom/wad.py` from the same seeded layout as the mini-Doom level: one sector, the box as the boundary, every `Wall` and `Obstacle` a solid pillar (rectangle / octagon, kept 2 map units off the boundary), no BSP nodes (ZDoom builds them on load), 1 world unit = 32 map units (player radius 16 = the body's 0.5 u). Things: player start, a green armour as the exit marker (harmless), Zombiemen as enemies with the AMBUSH flag on levels 3-4 (they wait until they see the player), free on 5-6. Textures from the doom2 name set (freedoom2 is the bundled IWAD). | Plan §24 "custom scenario, not a stock map", and the same seeded layout in both worlds gives a sim-vs-engine row for free (the Doom analogue of axis C8). A Doom monster always walks once awake: "stationary" is approximate. |
| 2026-09-16 | B4 | `VizdoomWorld(World)` mirrors the engine every tick (pose, health / 100, ammo, living monsters from the object list; a kill marks the monster nearest the gun line as a corpse, which stays in the list) and senses with the world's own code over the same walls and obstacles (rays, sectors, line of sight, `aim`); the engine owns motion, collisions, monsters, bullets and damage. Actions: `MOVE_FORWARD_BACKWARD_DELTA` = 20 x mean wheel (measured steady speed 6.6 map units = 0.206 u per tic at full drive, the simulator's 0.2), `TURN_LEFT_RIGHT_DELTA` = the world's angular step in degrees (exact per tic), `ATTACK` = the trigger; one tic per step. Events from game variables: hits `HITCOUNT`, kills `KILLCOUNT`, damage `DAMAGE_TAKEN`/`HEALTH`, a round fired = `AMMO2` decreased (the pistol fires a few tics after the press), the exit = within 1.1 u of the marker, collision = drive commanded and no motion. Known gaps: the engine has momentum (about 5 tics to speed, 1.9 u of coasting after release, none in the simulator), Doom monsters walk and shoot hitscan with random damage, the pistol refires every 14 tics (the simulator's cooldown is 5). | Plan §25-26 (game variables, the minimal action set; continuous thrust and turn rate are the differential drive expressed in Doom's own controls). The binary MOVE_FORWARD / TURN_* buttons were tried first: walking-speed turning (3.5 degrees per tic) made the player orbit the exit at 2.5 u. |
| 2026-09-16 | 23.3 | Axis C7 hybrid = a *frozen* brain under a trainable policy (`candidates/hybrid.py`): the seed's C6 curriculum worm runs every step with its weights fixed, its wheels (and trigger) reach a fresh small net as three extra channels `reflex_left/right/fire`, and only the net is in the genome (`n_weights` = the policy's). `--candidate hybrid --init-brain <brain>`: for this candidate `--init-brain` is the reflex, not a starting point. With the reflex dropped the row is exactly `rnn`, which is the control the axis needs. `load_candidate` now reads the *first* `kind` key of the file (a hybrid carries its policy's kind inside itself). | Plan §20.4 item 7 "a net from scratch plus the worm's reflexes as a fixed module", the cheapest of the three hybrids and the only one that needs no change to the harness, the benchmark or the layer (Plan §51: prefer the simpler option). Protocol copied from C6 (car preset, layer v2, 12 maps x 2 repeats, 3 seeds) so the row sits next to worm 44.4 ± 5.6 and PPO 37.5 ± 8.4. The "worm as a motivational layer over PPO" half of the axis is not built yet. |
| 2026-09-16 | B4 | `broomworm play --watch` / `benchmark --watch` (`vizdoom_world.set_watch`): the engine window opens (640x480, HUD, sound) and each step sleeps `tics/35` s, so an episode runs at Doom's own speed. Mode, seeds and everything the brain sees are unchanged, so a watched run reproduces the headless numbers. `make watch-doom LEVEL=doomN` (a trained brain) and `make watch-doomguy` (the hand-written floor, the only row that currently aims: 0.5 kills, 1.5 hits per episode against the trained brains' 0.0-0.2 kills on 50 shots). | Asked for by the owner: the tables say what happens, the window says what it looks like. Watching must not become a second code path (Plan §3.3): the window is a render flag on the same episode loop. |
| 2026-09-16 | Ops | Never edit the source while a background lane is running: an edit that is broken for 90 seconds (an import added after its first use) crashed six benchmark subprocesses that happened to start inside that window. The lanes are idempotent (a run is "done" only when its `.meta.json` / csv exists), so re-running the lane refills the gaps; the discipline is to finish edits, run `make check`, and only then launch. | Operational, found the hard way in the B2 resume lane (`runs/doom/resume.log`). |
| 2026-09-16 | 22.2 | Audit of the written-but-never-compiled ESP32 firmware against the host found two host-side defects that would have shown up on the car's first hour, both now fixed and covered: (1) `Calibration.channels()` served **no** `bumper_left/right` for the `car` preset, because it only looked at `cfg.bumper` and the car has `proximity_bumper` instead -> `broomworm selftest --link tcp` would report missing channels, and the layer's bumper reflex (`channels.get("bumper_left", 0.0)`) would read a permanent zero on the machine while the brain was trained with it working; (2) `wall_right` came out graded (0.5 for the firmware's own placeholder) with a 0.2 m trip point instead of the documented binary 1/0 at 0.15 m, i.e. a channel the brain never saw in training. Both derived paths now mirror `environments/sensors.py` (`Calibration.wall_channel`, `.proximity_bumper`), and `test_fake_robot_serves_the_car_preset` compares **every channel** over the wire against the simulator, not just the wheels. | Plan §20.5 "only Environment and adapters change": a channel the machine does not serve is a silent behaviour change, not an error. The wheels-only assertion passed for 30 steps while two channels were missing, which is why the test now diffs the whole dict. |
| 2026-09-16 | 22.2 | The self-test's odometry wiggle is *vacuous* on the car: with `odom_source="commands"` the odometry is integrated by the host from the wheel values the host itself sent, so the "did the heading turn the right way" check tests the host's arithmetic and would pass with the motor pairs crossed. `SelfTestReport.notes` now says so on any dead-reckoned preset (the vacuum preset, with encoders, keeps the plain claim), and the day-one runbook has to confirm each wheel pair by eye. | Plan §2.4 and the project's no-invention rule: a check that cannot fail must not be printed as if it could. |
| 2026-09-16 | 22.2 | Protocol robustness: the firmware answered nothing to an unparseable command (a fragmented Wi-Fi line is enough), while `LineLink._exchange` blocks on one reply per command -> a 10 s socket hang and then an uncaught `socket.timeout` in whatever loop was driving. The firmware now replies `{"error": ...}`, stops the wheels and keeps the link in step, the host turns that into a named `ValueError`, and a stalled machine into a `ConnectionError` instead of a hang; the idle branch of the firmware loop got `delay(1)` (a tight loop on the ESP32 `loopTask` trips the FreeRTOS task watchdog). Still unverifiable without the car: servo settle time (`delay(15)` is 10x too short for a hobby servo), ray-to-servo-angle direction, cliff/wall digital polarity, and the K210 beacon stub. | `fake_robot.py` was already the loud reference; the firmware disagreed with it on error semantics. The four remaining items are hardware measurements, listed as the first checks with the board in hand. |
| 2026-09-16 | B2/B11 | **Why the shuffled control beats the real connectome at shooting: a bottleneck we imposed, not a fact about wiring.** Measured on untrained brains (`doom4`, 3 maps x 200 ticks, before any evolution): the real connectome pulls the trigger in 0.0 % of ticks (mean fire 0.151), the degree-preserving shuffle in 77.0 % (0.679), the random topology in 34.8 %. Cause, checked in our own connectome: the trigger group is the pharyngeal pumping neurons M3/M4/MC (Plan §22's "bite = fire"), and the pharyngeal nervous system is an island -- exactly **5 connections cross its boundary, all through RIPL/RIPR**, and the trigger group's 17 input neurons are all pharyngeal (I1-I6, M1, M2, M5). No sensory channel can reach it. The shuffle destroys that island: 399 connections cross the boundary and the trigger gets input from 46 neurons including ADL (which carries `aim`) and the command interneurons AVA/AVB. Since the reward pays +2 per hit and +10 per kill, a brain that sprays gets a gradient and a brain that never fires gets none. | Consequence: every track-B row about shooting measures our motor mapping, not the connectome, and `docs/findings.md` says so. The fix is to give the real worm the same access honestly -- either a somatic trigger group that `aim` can reach, or `aim` into the pharynx through RIP, the animal's only bridge -- and re-measure (stage B2c). Plan §22 allows an artificial motor mapping; it does not allow comparing against a control that has one and a candidate that does not. |
| 2026-09-17 | B2d | **The second half of the shooting problem: the gun is bolted to the chassis, so aiming is turning, and nothing told the worm to turn toward a monster.** The enemy reached the brain only as `danger_*` -> ASHL/ASHR, the animal's nociceptive escape pathway, which turns it *away*; `aim` is unsigned (how centred, not which side), so it cannot steer either. New channels `prey_left/front/right`: the nearest *visible* enemy as a lateralised 1/distance attractant, live only while the gun is loaded (a monster you cannot shoot is a hazard, not prey). The receiving pair was picked by outcome, and the first pick was wrong. Scoring the 31 free sensory pairs by the *turn bias* they produce put ASG on top, but a bias is not a hit: scored instead by hits landed on a standing target from five bearings (200 ticks each, untrained connectome), CEPD takes 20 hits on 27 shots, PLM 12/22, ADE 4/20, ASG under 2, and the bare connectome 0. Any second pair added to CEPD destroys it (0 hits). On real levels, untrained: `doom4` −21.1 (ASG −30.5), `stock_defend` −5.4 with 0.50 kills and 0.75 survival (ASG −15.4 / 0.25 / 0.50). CEPD it is. The biology agrees: CEP are the dopaminergic head mechanosensors that report a lawn under the nose and slow the animal on food -- "prey right here, slow and turn onto it" is a behaviour they already own. Gain 3 (6 changes nothing: the graded activity clips at 1). | Plan §13 already admits artifices in the sensory table; this one is stated as what it is -- the enemy gets a second reading, as prey, so the worm's own taxis machinery can do the aiming. Verified not to touch the A track: without a gun the channels are zero, and the car benchmark reproduces to the last digit (37.000000 with and without both new routes). Trained rows: stage B2d lane. |
| 2026-09-17 | B2d | What the prey channel does and does not buy, measured on the untrained connectome with a standing target: **in front it hunts** -- from 10 u dead ahead it closes to 0.4 u and lands 20 hits, where the same worm without the channel lands none -- but **it cannot reorient**: with the target 60 degrees off the bow it only ends up closer (6.0 u against 8.5 u) and never brings the gun line onto it. The body's tonic turning asymmetry is larger than anything the sensory current can push. This is left to evolution rather than engineered away: the reflex is now expressible, the weights that sharpen it are the genome. In the animal a large reorientation is not a smooth turn either -- it is the pirouette, a reversal followed by an omega turn, driven by the gradient getting worse; our LIF neurons have leak but no explicit derivative, so that strategy is not available to it yet. | Plan §51 (prefer the simpler option) and the project's rule that the brain learns the behaviour: the mapping decides where a signal lands, never what to do with it. Recorded so the next stage does not re-measure it. |
| 2026-09-17 | 24 | **The vehicle left the brain's contract.** Until now every brain returned two wheel commands, because the first platform was a differential-drive vacuum: a Doom player has no wheels, and the car of Plan §20.5.1 has four mecanum ones that can also move sideways. A brain now returns `Drive(forward, turn, strafe, fire)` and a *body* turns that into actuators: `DifferentialDrive` -> (left, right), `MecanumDrive` -> four wheels, the engine -> its buttons. The platform's sensor preset names its vehicle (`car` is mecanum, the rest differential), `World.drive(intent)` is the one entry point and `World.step(left, right)` is now a thin differential wrapper. A brain from before the change still returns a pair and the loop reads it through `Drive.from_wheels`; the worm's motor adapter already computed drive and turn internally, so nothing was lost in translation. | Verified against the pre-change code in a separate worktree, same brains, same seeds: vacuum worm 35.6333333333, car worm 37.0000000000, vacuum rnn 26.5666666667, Doom worm −25.5333333333 with 44.67 shots -- identical to ten decimals on both sides. The only visible difference is the *logged* wheel pair, which round-trips through the intent and can differ by one ULP; the motion is bit-identical. Open: the 2D world moves sideways only for a body that strafes and no brain asks for it yet, the engine has no strafe button, and the worm has no lateral gait at all -- whether the nets get a fourth output the animal cannot have is a question for the matrix, not a bug. |
| 2026-09-17 | 23.0 | Mini-Doom removed from the platform: `Enemy`, hitscan, exit, the `fire`/trigger output of `Drive` and every candidate, `FireAdapter`, the pharyngeal `fire` motor group, the `aim`/`prey` sensory routes, `ammo`/`aim`/`prey_*` channels, kills/hits/shots/exited counters and the Doom leaderboard columns. `World.finished` is now just `dead`; `line_of_sight` and `segments` stay as geometry. Old brain JSONs still load (`gain_fire` in their params is accepted and ignored); old benchmark JSONs still load (unknown row fields are dropped). | Plan §6 stage 23.0. The Doom work lives in NorthArea/DoomWorm. |
| 2026-09-17 | 23.1 (desk) | Hardware inventory compiled from ACEBOTT's documentation and two unboxings, not from our box (`docs/hardware.md`, "Inventory from the documentation"): board ESP32 Max V1.0 (Arduino "ESP32 Dev Module", CH340, no BOOT button: GPIO0 to GND + RST to flash), shield QA052 with motor directions through a shift register (SHCP 18, STCP 17, DATA 5, EN 16) and speed on PWM 19, four 1:48 TT motors on mecanum wheels, HC-SR04, three-way line module, IR remote, 18650 holder (cells separate), QD003 = K210 vision module whose message format is unknown. Every line carries a "verified" column that stays empty until the box is open. | Plan §6 stage 23.1: the part that needs no hardware. The `car` preset keeps its numbers: nothing measurable changed. |
| 2026-09-17 | 23.2 (desk) | Firmware rewritten for the shield's shift-register driver (guessed bit map 128/64 FL, 32/16 FR, 8/4 RL, 2/1 RR marked TODO), made source-compatible with Arduino core for ESP32 2.x and 3.x (`ledcAttach` vs `ledcSetup`), and given a bench `probe` command; `broomworm motor-map` (`make motor-map LINK=fake\|tcp`) energises the eight direction bits one by one, takes the operator's answer (or the fake car's own), checks that every wheel has one bit per direction, and prints the two C tables to paste into the sketch. `arduino-cli`, the ESP32 core and ESP32Servo are installed project-locally under `.tools/` (git-ignored), `make arduino-setup`, `make firmware`, `make firmware-flash PORT=...`. | Plan §6 stage 23.2: everything that can be done before the car arrives. The bit map, EN polarity and a second PWM pin are day-one measurements. |
