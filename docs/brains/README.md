# Shared brains

The ancestors both tracks descend from. A "curriculum worm" row in either
track starts from a brain here, so it belongs to neither track alone.

| Folder | What it is |
|---|---|
| `curriculum/` | the connectome evolved on the food task — move toward a smell without hitting things. Stage 2 of the platform; every `worm_from_worm_evolved_random` row in either track began here. |

Each track's own trained brains live under `docs/<track>/brains/`.
