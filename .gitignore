# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.python-version.local

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# IDE / local tooling
.idea/
.vscode/
.mcp.json
.DS_Store

# PPO's training checkpoints, written every 30k steps. The final ppo.zip is
# the published brain and sits beside them; these are residue (audited by
# scripts/audit_brains.py, removed 2026-09-20)
ppo_ckpt/

# Experiment outputs (results are saved here, see docs/)
runs/
*.log

# ViZDoom engine settings written next to the process
_vizdoom.ini

# Doom data, fetched with scripts/fetch_freedoom.py (28 MB each)
data/wads/
*.wad
